import React from 'react';

const Header = () => (
  <header>
    <h1>Single-Seller Shop</h1>
    <nav>
      <a href="/">Home</a>
      <a href="/cart">Cart</a>
    </nav>
  </header>
);

export default Header;
